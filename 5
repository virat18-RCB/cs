Open the burp suit and select the proxy and select the open browser 
and come to come to Google Chrome and type /all labs
and create a a/c 
and select the os command 
come to burp suit and some code will be generated  
 in 20th line whoami
   -------------------------
   and http header 
    in the 21th line 
       <!DOCTYPE test [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
       and below the product id 
                     &xxe;
            and next step send to repeter :
                       and click on send :
                       and then change and put this code :
                       <!DOCTYPE test [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
    ____________________________________
    go to bwapp and select and login :
       and select the sql injection (get search)
        and paste the below line in the search option: 
             ramaiah'union select 1,login,email,password,secret,6,7 from users-- -
